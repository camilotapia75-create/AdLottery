# ── Database (Vercel Postgres / Neon) ──────────────────────────────
# Get these from your Vercel Postgres dashboard or neon.tech
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"
DIRECT_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# ── NextAuth ────────────────────────────────────────────────────────
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="generate-a-random-secret-with-openssl-rand-base64-32"
NEXTAUTH_URL="https://your-app.vercel.app"

# ── Google AdSense (optional — for real ad revenue) ─────────────────
# Get these from: https://adsense.google.com → Ads → By ad unit
NEXT_PUBLIC_ADSENSE_CLIENT="ca-pub-XXXXXXXXXXXXXXXXX"
NEXT_PUBLIC_ADSENSE_SLOT="XXXXXXXXXX"
